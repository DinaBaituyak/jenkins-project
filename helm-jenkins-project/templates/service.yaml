apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-jenkins-project.fullname" . }}
  labels:
    {{- include "helm-jenkins-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-jenkins-project.selectorLabels" . | nindent 4 }}
